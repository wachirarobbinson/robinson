import React from 'react';
import ReactDOM from 'react-dom';
import AuthComponent from './AuthComponent'; // Replace with the correct path to your JSX component file
import "./index.css"

ReactDOM.render(
  <React.StrictMode>
    <AuthComponent />
  </React.StrictMode>,
  document.getElementById('app')
);
